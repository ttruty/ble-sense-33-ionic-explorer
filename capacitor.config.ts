import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ble_direction',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
